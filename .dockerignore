# Dependencies (installed in container)
node_modules
**/node_modules

# Build output (built in container)
client/dist
server/dist

# Version control
.git

# Environment and secrets
.env*

# Runtime data (mounted as volumes, not baked into image)
server/uploads
server/data

# Logs
*.log

# Documentation (not needed in image)
README.md

# Local dev config and tooling
docker-compose.yml
.vscode
.idea
.DS_Store
*.local
AGENTS.md
client/eslint.config.js
