services:
  playblast:
    build: .
    ports:
      - "3000:3000"
    environment:
      UPLOAD_DIR: /app/uploads
    volumes:
      - playblast_uploads:/app/uploads
    restart: unless-stopped

volumes:
  playblast_uploads:
