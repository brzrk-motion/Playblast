# API/UI test matrix

Source of truth: `@playblast/shared` (`test-matrix.ts`).

## Capability matrix

- 16 capabilities × 3 roles = **48 cases**
- Generated by `buildCapabilityTestMatrix()`
- Admin must allow every capability granted to Creative or Proofing

## Proofing deny highlights

| Capability | Proofing |
|------------|----------|
| `setup.complete` | deny |
| `team.manage` | deny |
| `projects.mutate` | deny |
| `media.upload` | deny |
| `media.version` | deny |
| `approval.mutate` | deny |
| `data.delete` | deny |
| `comments.create` | allow |
| `annotations.create` | allow |
| `downloads.read` | allow |

## Route matrix

`buildRouteTestMatrix()` covers implemented client routes × 3 roles. CRM routes deny Creative and Proofing.

## Navigation matrix

`buildNavTestMatrix()` covers main nav items × 3 roles. CRM and Team items hidden for non-Admin roles.

## Fixtures

| Role | Email | User id |
|------|-------|---------|
| Admin | `admin@fixture.studio` | `user-fixture-admin` |
| Creative | `creative@fixture.studio` | `user-fixture-creative` |
| Proofing | `proofing@fixture.studio` | `user-fixture-proofing` |

Studio: `studio-fixture-1` / `Fixture Studio`

Use fixtures in Phase 1+ integration tests; Phase 0 verifies fixture shape and matrix generation only.
